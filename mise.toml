[tasks."brew:sync"]
description = "Sync installed Homebrew packages into the Brewfile"
run = "./scripts/sync-brewfile"

[tasks."mason:update"]
description = "Install and update the persisted Mason tool inventory"
run = "nvim --headless '+MasonToolsUpdateSync' '+qa'"
