# This file is generated by chezmoi.
# Define data.secretEnvByProfile in ~/.config/chezmoi/chezmoi.toml.
{{- $profile := default "personal" .profile -}}
{{- $secretRefs := index (default (dict) .secretEnvByProfile) $profile -}}
{{- range $name, $ref := $secretRefs }}
export {{ $name }}="{{ output "op" "read" $ref }}"
{{- end }}
