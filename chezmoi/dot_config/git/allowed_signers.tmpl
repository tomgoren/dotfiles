# This file is generated by chezmoi.
# Signing keys are read from data.gitIdentityByProfile in ~/.config/chezmoi/chezmoi.toml.
{{ range $profile, $identity := default (dict) .gitIdentityByProfile -}}
{{ if and (hasKey $identity "email") (hasKey $identity "signingkey") -}}
{{ $identity.email }} namespaces="git" {{ $identity.signingkey }}
{{ end -}}
{{ end -}}
